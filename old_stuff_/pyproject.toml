[project]
name = "mm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "matplotlib",
    "numpy",
]

[project.scripts]
mm = "mm.cli.main:main"
